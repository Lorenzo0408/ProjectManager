<Window x:Class="ProjectManager.View.DeleteConfirmationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        Width="420" Height="220"
        WindowStartupLocation="CenterScreen"
        Opacity="0"
        Icon="pack://application:,,,/Assets/PML.ico">

    <!-- Animation d'apparition -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.25"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border Background="#1E1E1E" CornerRadius="14" Padding="20" BorderBrush="#444" BorderThickness="1">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock x:Name="MessageText"
                       Foreground="White"
                       FontSize="20"
                       FontWeight="Bold"
                       TextAlignment="Center"
                       Margin="0 0 0 20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">

                <Button Content="No"
                        Width="130" Height="38"
                        Background="#333"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderBrush="#555"
                        Click="Cancel_Click"/>

                <Button Content="Yes"
                        Width="130" Height="38"
                        Background="#AA0000"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderBrush="#550000"
                        Click="Confirm_Click"/>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
