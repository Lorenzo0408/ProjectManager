<Window x:Class="ProjectManager.View.TaskCreatorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectManager.View"
        mc:Ignorable="d"
        Title="TaskCreatorView" Height="450" Width="800"
        Background="#1E1E1E"
        Icon="pack://application:,,,/Assets/PML.ico"
        xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.3*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Create a new Task :" Grid.Column="0" Grid.Row="0" FontSize="24" Style="{StaticResource UnderlinedText}" Grid.ColumnSpan="2"/>
        <Label Content="Title of the Task : " Grid.Column="0" Grid.Row="1" Margin="0, 35" FontSize="18" Style="{StaticResource LabelStyle}"/>
        <Label Content="Description of the Task :" Grid.Column="0" Grid.Row="2" FontSize="18" Style="{StaticResource LabelStyle}" Grid.ColumnSpan="2"/>
        <Label Content="Estimated Time (hours) : " Grid.Column="1" Grid.Row="2" FontSize="18" Style="{StaticResource LabelStyle}" Grid.ColumnSpan="3" Margin="235,24,5,84" Grid.RowSpan="2"/>
        <Label Content="Due Date : " Grid.Column="2" Grid.Row="1" FontSize="18" Style="{StaticResource LabelStyle}"/>
        <Label x:Name="LabelPriority" Content="Priority : " Grid.Column="2" Grid.Row="1" FontSize="18" Style="{StaticResource LabelStyle}" Margin="0,79,0,30" Grid.RowSpan="2"/>
        <TextBox Text="{Binding TaskTitle}" Grid.Column="1" Grid.Row="1" Margin="0, 30, 50, 30" Style="{StaticResource ModernTextBox}"/>
        <TextBox Text="{Binding TaskDescription}" Grid.Column="1" Grid.Row="2" Margin="25,30,5,65" Style="{StaticResource ModernTextBox}" Grid.RowSpan="3" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        <TextBox Text="{Binding TaskTempsEstime}" Grid.Column="3" Grid.Row="2" Margin="0,65,50,103" Style="{StaticResource ModernTextBox}" PreviewTextInput="NumberOnly" Grid.RowSpan="2"/>
        <DatePicker SelectedDate="{Binding TaskEcheance, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue={x:Static sys:DateTime.Now}}" Grid.Column="3" Grid.Row="1" Margin="0, 30, 50, 30"/>
        <Slider x:Name="SliderVal" Value="{Binding TaskImportance}" Grid.Column="3" Grid.Row="1" Margin="0,85,50,84" ValueChanged="Slider_ValueChanged" Grid.RowSpan="2"/>
        <Button Content="Create Task" Grid.Column="1" Grid.Row="4" Margin="145,10,65,10" Style="{StaticResource RoundedButton}" Grid.ColumnSpan="2" Command="{Binding CreateTaskCommand}"/>
        <Label Content="Choose a color : " Grid.Row="2" Grid.Column="0" FontSize="18" Style="{StaticResource LabelStyle}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="0,0,0,75" Grid.RowSpan="2"/>
        <ComboBox Grid.Row="3" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10,0,0,57" RenderTransformOrigin="2.684,0.489" ItemsSource="{Binding AvailableColors}" SelectedItem="{Binding SelectedColor}" Width="150">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="16" Height="16" Fill="{Binding}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBlock Grid.Column="2" HorizontalAlignment="Left" Margin="10,35,0,0" Grid.Row="3" TextWrapping="Wrap" Text="Task Owner : " VerticalAlignment="Top" Foreground="White" FontSize="18" FontWeight="Bold" FontStyle="Oblique"/>
        <TextBox Grid.Column="3" HorizontalAlignment="Left" Margin="0,31,0,0" Grid.Row="3" TextWrapping="Wrap" Text="{Binding TaskOwner}" VerticalAlignment="Top" Width="190" Style="{StaticResource ModernTextBox}" Height="50"/>
    </Grid>
</Window>
